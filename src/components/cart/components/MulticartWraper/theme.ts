export interface MultiCartWrapperTheme {
  wrapper: Wrapper;
  text: Text;
}
interface Text {
  shopping: string;
  empty: string;
}

interface Wrapper {
  main: string;
  height: string;
  sticky: string;
}
